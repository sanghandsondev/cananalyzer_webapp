# CAN Analyzer Web App

1. Clone project
git clone <repo-url>
cd cananalyzer_webapp

2. Install dependencies
npm install

3. Create .env file from .env.example & fill variables for .env file
cp .env.example .env
Example: PORT=3000

4. Run applicaiton
- If develop environment: npm run dev
- If production environment: npm start