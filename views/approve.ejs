<!doctype html>
<html lang="vi">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Kết quả thanh toán - CAN Analyzer</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div id="resultContainer" class="result-container" data-order-id="<%= orderId %>" data-status="<%= status %>">
      <% if (status === 'COMPLETED') { %>
        <h1 id="statusTitle" class="result-title--success">Thanh toán thành công!</h1>
      <% } else if (status === 'PENDING') { %>
        <h1 id="statusTitle" class="result-title--pending">Thanh toán đang chờ xử lý</h1>
      <% } else { %>
        <h1 id="statusTitle" class="result-title--fail">Thanh toán thất bại</h1>
      <% } %>
      
      <p id="statusMessage" class="result-message"><%= message %></p>
      
      <a href="/" class="btn-home">Trở về Trang chủ</a>
    </div>

    <script src="/js/approve.js"></script>
  </body>
</html>
